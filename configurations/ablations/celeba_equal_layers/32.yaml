model:
  hidden_sizes: [32, 32, 32, 32]
  modulator_params:
    hidden_sizes: [16, 16]

fitting:
  tuning:
    log_interval: 10
    iterations: &epoch_iterations 300
    scheduler:
      T_0: *epoch_iterations
 