model:
  hidden_sizes: [64, 64, 64, 64]
  modulator_params:
    hidden_sizes: [24, 24, 24]

fitting:
  tuning:
    log_interval: 10
    iterations: &epoch_iterations 300
    scheduler:
      T_0: *epoch_iterations
 